# Инструкция по настройке модуля "Обработка пересдач внешней оценки"

## 📋 Содержание
1. [Настройка Supabase](#настройка-supabase)
2. [Подготовка данных](#подготовка-данных)
3. [Использование модуля](#использование-модуля)

---

## 🗄️ Настройка Supabase

### Шаг 1: Создание таблицы `students`

Выполните следующий SQL-запрос в **SQL Editor** в Supabase Dashboard:

```sql
CREATE TABLE IF NOT EXISTS students (
    id BIGSERIAL PRIMARY KEY,
    "ФИО" TEXT NOT NULL,
    "Адрес электронной почты" TEXT UNIQUE NOT NULL,
    "Филиал (кампус)" TEXT,
    "Факультет" TEXT,
    "Образовательная программа" TEXT,
    "Группа" TEXT,
    "Курс" TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Создание индекса для быстрого поиска по email
CREATE INDEX IF NOT EXISTS idx_students_email ON students("Адрес электронной почты");
```

### Шаг 2: Создание таблицы `peresdachi`

Выполните SQL-скрипт из файла `create_peresdachi_table.sql`:

```bash
# Откройте файл create_peresdachi_table.sql и скопируйте содержимое
# Вставьте в SQL Editor в Supabase Dashboard и выполните
```

Или выполните этот код напрямую:

```sql
CREATE TABLE IF NOT EXISTS peresdachi (
    id BIGSERIAL PRIMARY KEY,
    "ФИО" TEXT,
    "Адрес электронной почты" TEXT,
    "Кампус" TEXT,
    "Факультет" TEXT,
    "Образовательная программа" TEXT,
    "Группа" TEXT,
    "Курс" TEXT,
    "ID дисциплины" TEXT,
    "Наименование дисциплины" TEXT,
    "Период аттестации" TEXT,
    "Оценка" TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_peresdachi_email ON peresdachi("Адрес электронной почты");
CREATE INDEX IF NOT EXISTS idx_peresdachi_discipline ON peresdachi("Наименование дисциплины");
CREATE INDEX IF NOT EXISTS idx_peresdachi_fio ON peresdachi("ФИО");
```

### Шаг 3: Загрузка данных студентов

Импортируйте данные студентов из файла `Список студентов.xlsx` в таблицу `students`:

**Вариант 1: Через Supabase Dashboard**
1. Откройте таблицу `students` в Table Editor
2. Нажмите "Insert" → "Import from CSV/Excel"
3. Выберите файл `Список студентов.xlsx`
4. Сопоставьте колонки

**Вариант 2: Через Python скрипт**
```python
import pandas as pd
from supabase import create_client

# Подключение к Supabase
url = "your_supabase_url"
key = "your_supabase_key"
supabase = create_client(url, key)

# Чтение файла
df = pd.read_excel('Список студентов.xlsx')

# Выбор нужных колонок
students_data = df[[
    'ФИО', 
    'Адрес электронной почты', 
    'Филиал (кампус)', 
    'Факультет', 
    'Образовательная программа', 
    'Группа', 
    'Курс'
]].dropna(subset=['Адрес электронной почты'])

# Загрузка в Supabase
records = students_data.to_dict('records')
supabase.table('students').insert(records).execute()
```

---

## 📝 Подготовка данных

### Требования к файлу с оценками

Файл должен содержать следующие колонки:
- **Адрес электронной почты** - email студента (обязательно)
- **Тест:Входное тестирование (Значение)** - оценка входного тестирования
- **Тест:Промежуточное тестирование (Значение)** - оценка промежуточного тестирования
- **Тест:Итоговое тестирование (Значение)** - оценка итогового тестирования

**Пример структуры файла:**
```
| Фамилия | Имя | Адрес электронной почты | Тест:Входное тестирование (Значение) | ... |
|---------|-----|------------------------|--------------------------------------|-----|
| Иванов  | Иван| ivanov@example.com     | 8                                    | ... |
```

---

## 🚀 Использование модуля

### Шаг 1: Запуск приложения

```bash
streamlit run app.py
```

### Шаг 2: Выбор модуля

В боковом меню выберите:
```
📝 Обработка пересдач внешней оценки
```

### Шаг 3: Загрузка файла

1. Убедитесь, что подключение к Supabase установлено (зелёная галочка)
2. Проверьте, что список студентов загружен из Supabase
3. Загрузите файл с оценками через форму загрузки

### Шаг 4: Обработка данных

1. Нажмите кнопку **"🚀 Обработать данные"**
2. Дождитесь завершения обработки
3. Система автоматически:
   - Обработает оценки
   - Сохранит результаты в таблицу `peresdachi`
   - Определит новые записи

### Шаг 5: Скачивание результатов

После обработки вам будут доступны две опции:

**📋 Вкладка "Все обработанные данные"**
- Показывает все обработанные записи из текущего файла
- Кнопка "📥 Скачать все записи (XLSX)"

**🆕 Вкладка "Только новые записи"**
- Показывает только те записи, которых ещё не было в базе данных
- Кнопка "📥 Скачать только новые записи (XLSX)"

---

## 📊 Что делает система

1. **Очистка данных**: Удаляет символы "-" и лишние пробелы
2. **Переименование колонок**: Приводит названия к стандартному виду
3. **Трансформация (Melt)**: Преобразует колонки с оценками в строки
4. **Объединение данных**: Добавляет информацию о студентах из Supabase
5. **Сохранение в БД**: Записывает результаты в таблицу `peresdachi`
6. **Определение новых записей**: Находит записи, которых ещё не было в базе

---

## 🔍 Структура итоговых данных

Итоговая таблица содержит следующие колонки:

1. **ФИО** - полное имя студента
2. **Адрес электронной почты** - email студента
3. **Кампус** - кампус НИУ ВШЭ
4. **Факультет** - факультет студента
5. **Образовательная программа** - программа обучения
6. **Группа** - учебная группа
7. **Курс** - курс обучения
8. **ID дисциплины** - идентификатор дисциплины (пустое поле для заполнения)
9. **Наименование дисциплины** - название дисциплины внешней оценки
10. **Период аттестации** - период аттестации (пустое поле для заполнения)
11. **Оценка** - полученная оценка

---

## ⚠️ Возможные проблемы и решения

### Проблема: "Ошибка подключения к Supabase"
**Решение**: Проверьте файл `secrets.toml` - убедитесь, что URL и KEY указаны корректно

### Проблема: "Список студентов пуст"
**Решение**: Загрузите данные студентов в таблицу `students` в Supabase

### Проблема: "Таблица peresdachi не найдена"
**Решение**: Выполните SQL-скрипт для создания таблицы `peresdachi`

### Проблема: "Нет новых записей"
**Решение**: Это нормально, если все данные уже были загружены ранее

---

## 📧 Контакты

При возникновении вопросов обращайтесь к разработчику: **Тимошка** 💚
